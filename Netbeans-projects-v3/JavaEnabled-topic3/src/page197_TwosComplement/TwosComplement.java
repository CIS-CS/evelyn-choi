package page197_TwosComplement;

/**
 * Negate a number using two's complement format.
 * @author Mark Hayes
 */
public class TwosComplement {

    /** Entry point to program. */
    public static void main(String[] args) {

        // Input a binary number into a string
        // Visit each bit in the input string using charAt()
        // From L to R, flip (invert) each bit into a StringBuffer
        // Convert the StringBuffer back to string

        // Add one to the flipped string
        // Keep track of the carry value
        // Visit each bit R to L in the flipped string
        // Add the carry bit
    }

}
